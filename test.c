#include "Stack.h"
#include "Queue.h"

int main()
{
	//DataType tmp = 0;
	//Stack s;
	//StackInit(&s); 
	//StackPush(&s, 1); 
	//StackPush(&s, 2);
	//StackPush(&s, 3); 
	//StackPush(&s, 5);
	//StackPush(&s, 3);
	////StackDestory(&s);
	////tmp = StackTop(&s);
	////printf("栈顶的元素是：%d", tmp);
	//if (!StackEmpty(&s))
	//{
	//	printf("栈为空\n");
	//}
	//else
	//{
	//	printf("栈非空\n");
	//}
	//tmp = StackSize(&s);
	//printf("栈中元素个数：%d", tmp);
	//TestQueue();
	TestQueueByTwoStack();

	system("pause");
	return 0;
}
